# Local secrets (never commit)
leadengine-*.json
*service-account*.json
*.env
# --- secrets (never commit) ---
leadengine-*.json
*service-account*.json
*.pem

# --- env files ---
.env
.env.*
.env.local
.env.vercel

# --- build / tool state ---
node_modules/
.next/
.vercel/
*.tsbuildinfo
